<?xml version="1.0" encoding="UTF-8"?>
<TEI xmlns="http://www.tei-c.org/ns/1.0" xml:id="D<%= entity.ref_id %>">
    <teiHeader>
      <%= render :partial => 'entities/tei_file_desc', :locals => { :entity => entity } %>
        <encodingDesc>
            <listPrefixDef xmlns:xi="http://www.w3.org/2001/XInclude" xml:id="inc_global_prefixDef">
                <prefixDef ident="cwgk" matchPattern="(.+)" replacementPattern="$1.xml">
                </prefixDef>
            </listPrefixDef>
        </encodingDesc>
        <profileDesc>
            <textClass>
                <keywords>
                    <term type="type">Place</term>
                </keywords>
            </textClass>
            <settingDesc>
                <place xml:id="<%= entity.ref_id%>">
                    <placeName>
                        <location>
                            <% if entity.lat %>
                              <geo><%= entity.lat %> <%= entity.long %></geo>
                            <% end %>
                            <settlement><%= entity.name %></settlement>
                        </location>
                    </placeName>
                </place> 
            </settingDesc>    
        </profileDesc>
    </teiHeader>
    <%= render :partial => 'entities/tei_text', :locals => { :entity => entity } %>
</TEI>