<h1>Annotations for <%= @annotations.first.document.title if @annotations.first.document %></td></h1>

<table>
  <thead>
    <tr>
      <th>Verbatim</th>
      <th>Entity</th>
      <th>Hypothesis annotation</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @annotations.each do |annotation| %>
      <tr>
        <td><%= annotation.verbatim %></td>
        <td><%= annotation.entity.name if annotation.entity %></td>
        <td><%= annotation.hypothesis_annotation_id %></td>
        <td><%= link_to 'Identify', identify_annotation_path(annotation) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Next --> Create Relationships', define_relationships_path %>
